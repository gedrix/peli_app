name: cloncines
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0

dev_dependencies:
  dio: ^5.3.2
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  go_router: ^10.1.2

flutter:
  uses-material-design: true

  assets:
    - .env
